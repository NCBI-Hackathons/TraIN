brainfinal <- read.csv(file = "../outputs/brain_ppi_mapping.csv")
immunofinal <- read.csv(file = "../outputs/immuno_ppi_mapping.csv")

for (i in 1:nrow(brainfinal)){
  if (brainfinal$Ensembl_GeneID_A==)
}